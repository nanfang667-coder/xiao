// Temporary free-access mode: payments stay off until this code-level switch is restored.
export const PAYMENT_FEATURE_ENABLED = false;
// Temporary hidden mode: remove the entry and reject direct public access.
export const ALLEY_PUBLIC_ENABLED = false;
export const ALLEY_DIRECT_ACCESS_ENABLED = false;
